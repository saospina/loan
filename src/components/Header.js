import React from 'react';


/* function Header(props) {
    console.log(props);

    return (
        <Fragment>

            <h1>{props.tittle}</h1>

        </Fragment>
    )
}; */

const Header = (props) =>
    (
        <h1>{props.tittle}</h1>

    );




export default Header;